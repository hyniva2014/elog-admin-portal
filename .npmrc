# Always use the public npm registry for all packages
registry=https://registry.npmjs.org/
# Disable package-lock.json to prevent version conflicts
package-lock=false
# Disable package-lock.json to prevent version conflicts
legacy-peer-deps=true
script-shell=C:\\Windows\\System32\\cmd.exe
